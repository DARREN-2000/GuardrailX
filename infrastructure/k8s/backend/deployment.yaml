apiVersion: apps/v1
kind: Deployment
metadata:
  name: guardrailx-backend
  labels:
    app: guardrailx-backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: guardrailx-backend
  template:
    metadata:
      labels:
        app: guardrailx-backend
    spec:
      containers:
        - name: backend
          image: guardrailx-backend:latest
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: guardrailx-backend-config
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: guardrailx-db-secret
                  key: database_url
          livenessProbe:
            httpGet:
              path: /api/v1/health/live
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /api/v1/health/ready
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
